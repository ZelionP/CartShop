const reducer = (state, action) =>{
//    if (action.type === "CLEAR_CART") {
//        console.log(state)
//     return {...state, cart: []}   
//    }
//    if (action.type === "REMOVE") {
//        return(
//            ...state,
//            cart: state.cart.filter(item => item.id !== ation.payload),
//
//        )
//    }
//    return new Error("this type of action does not exist!")
//
}

export default reducer